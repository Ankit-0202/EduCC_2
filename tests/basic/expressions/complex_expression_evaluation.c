int main() {
    int a = 8;
    int b = 3;
    int c = 2;
    int d = 4;
    // Expression: a / b * c + d - (a - c)
    // Integer division: 8 / 3 = 2, so:
    // (2 * 2) + 4 - (8 - 2) = 4 + 4 - 6 = 2.
    return ( (a / b) * c + d - (a - c) ) - 2;
}
